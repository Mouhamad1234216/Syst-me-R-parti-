### Backend

Flask app exposing `/api/users` and `/api/products`.

Run locally (venv):

```powershell
python -m venv venv
venv\Scripts\Activate.ps1
pip install -r backend\requirements.txt
python backend\app.py
```

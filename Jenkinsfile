pipeline {
  agent any
  stages {
    stage('Lint & Test') {
      steps { echo 'Run linter and tests (implement)'}
    }
    stage('Build Images') {
      steps { echo 'Build Docker images (implement)'}
    }
    stage('Push') {
      steps { echo 'Push to registry (implement)'}
    }
    stage('Deploy') {
      steps { echo 'Deploy to Kubernetes (implement)'}
    }
  }
}
